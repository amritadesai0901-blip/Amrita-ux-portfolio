# Amrita Dhakras — Portfolio

Built with React + Vite.

## Local dev
```bash
npm install
npm run dev
```

## Deploy
Push to `main` — GitHub Actions auto-builds and deploys to GitHub Pages.

> Set repo name in `vite.config.js` → `base: '/your-repo-name/'`
